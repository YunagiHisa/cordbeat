[project]
name = "cordbeat-skill-fetch-url"
version = "0.1.0"
description = "CordBeat builtin skill: SSRF-hardened URL fetcher with HTML-to-text conversion"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]
